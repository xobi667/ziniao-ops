interface:
  display_name: "Ziniao Ops"
  short_description: "Operate local seller stores through Ziniao"
  default_prompt: "Use $ziniao-ops to open or operate a local seller store through Ziniao, inspect operations pages, or create a read-only report."

policy:
  allow_implicit_invocation: true
