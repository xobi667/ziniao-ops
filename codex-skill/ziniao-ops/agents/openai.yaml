interface:
  display_name: "Ziniao Ops"
  short_description: "Run local Ziniao and Xinjian shop commands"
  default_prompt: "Use $ziniao-ops to run local Ziniao or Xinjian commands, open/reuse Xinjian pages, analyze ad hourly data, watch exports, or inspect store operations."

policy:
  allow_implicit_invocation: true
