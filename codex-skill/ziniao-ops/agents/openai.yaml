interface:
  display_name: "Ziniao Ops"
  short_description: "Operate local seller stores through Ziniao and map ecommerce tool/API routes"
  default_prompt: "Use $ziniao-ops to operate a local seller store through Ziniao, guide the next clicks, check operations data, map ecommerce platform APIs and external tools, or create a read-only operations report on this computer."

policy:
  allow_implicit_invocation: true
