# Installed Skill Recovery Snapshot

`installed-skill-20260723/` is a recovery snapshot of the locally installed
`ziniao-ops` Codex skill captured on 2026-07-23.

The configured full source-package directory was no longer present when this
backup was made. This snapshot therefore contains the installed skill,
agent metadata, public references, and reference hash manifest only. It is not
a replacement for the complete package source, installers, or runtime scripts.

The repository root remains the last available complete source tree. Verify the
reference manifest before using this snapshot for recovery.
